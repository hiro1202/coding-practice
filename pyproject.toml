[project]
name = "coding-practice"
version = "0.1.0"
description = "コーディング練習用リポジトリ"
requires-python = ">=3.12"

[dependency-groups]
dev = ["pytest>=8"]

[tool.pytest.ini_options]
testpaths = ["contests"]
# 各問題に同名の test_main.py を置けるようにする
addopts = "--import-mode=importlib"
