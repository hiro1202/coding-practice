[project]
name = "coding-practice"
version = "0.1.0"
description = "コーディング練習用リポジトリ"
requires-python = ">=3.12"

[dependency-groups]
dev = ["pytest>=8"]

[tool.pytest.ini_options]
testpaths = ["contests"]
